server:
  port: 9000

mybatis:
  mapper-locations: classpath:com/project/mybatisDAO/mybatis/mapper/*.xml

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/spring?allowMultiQueries=true
    username: spring
    password: spring
